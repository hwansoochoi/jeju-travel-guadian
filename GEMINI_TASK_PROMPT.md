# [작업 지시] 스마트 제주 트래블 가디언 - 코드/문서 정합성 개선 및 보강

## 0. 역할과 컨텍스트

너는 이 프로젝트의 시니어 프론트엔드 엔지니어 겸 사업제안서 검토자다.
아래 프로젝트를 **직접 파일을 수정**해서 개선하라.

**작업 디렉터리**: `/Users/hwansoochoi/.gemini/antigravity/scratch/safe-travel-guardian`

**프로젝트 성격**: 제주도 단독여행자 대상 안전·구난 플랫폼의 **사업 제안용 프레젠테이션 덱 + 기술명세서**.
지자체/소방청 대상 발표 및 심사 제출이 목적이다. 실제 앱 코드가 아니라 **제안 자료**다.

**파일 구성**:
| 파일 | 내용 |
|---|---|
| `index.html` | 약 1,880줄. **인라인 CSS+JS 단일 파일** 18장짜리 인터랙티브 슬라이드 덱. 슬라이드 11은 Web Audio API를 쓰는 라이브 시뮬레이터 |
| `TECHNICAL_SPEC.md` | 약 330줄. iOS/Android 명세, OS별 법적·기술적 제약 매트릭스, 32MM 공수 산정, 6개월 WBS, OpenAPI 연동 규격 |
| `README.md` | 4대 영역 16대 기능 목록, Mermaid 시스템 구조도, OpenAPI 레지스트리 표 |
| `cloudflare/worker.js` | `index.html` 전체를 자바스크립트 문자열로 **통째 복제**해 서빙하는 Worker |
| `vercel.json` | `{"cleanUrls": true}` 뿐 |

**절대 지켜야 할 제약**:
- `index.html`은 **빌드 없이 브라우저에서 바로 열리는 단일 파일**이어야 한다. 외부 CSS/JS/CDN 의존성을 추가하지 마라.
- 기존 다크 테마 디자인 토큰(`--bg-main`, `--emerald`, `--rose` 등)과 시각적 톤을 그대로 유지하라. 리디자인 금지.
- 모든 사용자 노출 텍스트는 **한국어**로 유지하라.
- 슬라이드 개수(18장)와 순서를 바꾸지 마라.
- 각 단계 수정 후 브라우저에서 열어 콘솔 에러가 없는지 확인하라.

---

## 1. [P0] 성범죄자 알림e 기능 - 문서 내 정면 충돌 해소 (최우선)

### 문제
같은 덱 안에서 동일 기능에 대해 정반대 주장을 하고 있다. 심사에서 즉시 지적당한다.

**"핵심 기능이다"라고 주장하는 곳**:
- `index.html:800` — 커버 슬라이드 부제 "일출몰·만조·우범지대·**성범죄자 알림** & 가짜 통화"
- `index.html:834` — 슬라이드 2 "우범지대나 **성범죄자 주거지** 통과 시 사전 경각심이 없고..."
- `index.html:852` — 슬라이드 3 카드 "**성범죄자 알림e 연계** / 반경 200m 접근 시 지능형 사전 주의 가이드"
- `index.html:926~933` — 슬라이드 5 전체 블록 "**성범죄자 주거지 접근 알림**", "여성가족부 성범죄자 알림e 데이터 기반", "데이터 출처: 여성가족부 성범죄자 알림e 공개정보"
- `index.html:1207~1208` — 슬라이드 11 시뮬레이터 버튼 "🛡️ 성범죄자 주거지 반경 접근 / 여가부 알림e"
- `index.html:1291` — 슬라이드 12 "경찰청 범죄지도, **성범죄자 알림**, 택시·렌터카 e-Call..."
- `index.html:1996~1997` — `triggerSexOffenderAlert()` 함수의 로그/alert 문구
- `README.md:14` — 4대 영역 1번 항목 3 "**인근 성범죄자 주거지 안내**"

**"법적으로 불가능하다"고 판정한 곳**:
- `index.html:1516` — 슬라이드 14 매트릭스 4행: "⚠️ **법적 전면 불가 (아청법 위반)** / 아동·청소년의 성보호에 관한 법률 제55조 제5항 위반 시 5년 이하의 징역 또는 5천만원 이하의 벌금"
- `TECHNICAL_SPEC.md:212` — 동일 내용
- `TECHNICAL_SPEC.md:223` — "성범죄자 개별 이름, 얼굴, 세부 번지수 표출을 시스템 레벨에서 원천 배제"

### 판단 기준
**슬라이드 14 / TECHNICAL_SPEC의 법적 판단이 옳다.** 성범죄자 알림e 공개정보를 앱에서 위치 기반으로 재가공·알림 발송하는 것은 아청법 제55조 5항의 "공개정보 목적 외 이용"에 해당한다. "프라이버시 보호형"이라는 수식어를 붙여도 위법성이 사라지지 않는다.

### 지시
위 "핵심 기능이다" 목록 **전부**를 다음 합법 대안으로 리네이밍·재작성하라.

- **새 기능명**: `여성안심귀갓길 & 5대 범죄 밀집도 지오펜스`
- **데이터 출처**: 행정안전부·국립재난안전연구원 **생활안전지도(Safemap) OpenAPI** — 5대 범죄 발생통계 WMS 레이어, 여성안심귀갓길 폴리곤, 야간 조도 취약구간
- **동작 설명**: 특정 개인의 주거지가 아니라 **행정구역 단위 통계 기반 위험등급 폴리곤**(안전/주의/경고) 진입 시 햅틱 경고 및 안심귀갓길 우회 안내
- `triggerSexOffenderAlert()` 함수는 `triggerSafeReturnZoneAlert()`로 이름을 바꾸고, 로그/alert 문구도 위 내용에 맞춰 다시 쓰라. 호출부(onclick)도 함께 수정하라.
- `README.md:14`의 4대 영역 항목 3도 동일하게 교체하라.

**추가로**: 슬라이드 14 매트릭스 4행과 `TECHNICAL_SPEC.md`의 해당 행은 **그대로 두되**, 우회 방안 설명이 이제 실제 구현과 일치한다는 점이 드러나도록 문구를 다듬어라. (덱을 처음부터 끝까지 읽으면 "법적 리스크를 사전 검토해 합법 설계로 전환했다"는 일관된 서사가 되어야 한다. 이건 오히려 심사 가점 요소이므로 숨기지 말고 강점으로 서술하라.)

---

## 2. [P0] 스페이스바 슬라이드 이동 버그

`index.html:1837` 부근:
```js
if (e.key === 'ArrowRight' || e.key === 'Space') {
```
`KeyboardEvent.key`의 스페이스바 값은 `'Space'`가 아니라 `' '`(공백 한 칸)다. `'Space'`는 `e.code`의 값이다. 따라서 현재 스페이스바를 누르면 슬라이드가 넘어가지 않고 페이지만 스크롤된다. `README.md` 실행 방법에는 "[Space]로 이동 가능"이라고 적혀 있어 문서와도 불일치다.

**수정**: `e.key === ' ' || e.code === 'Space'` 로 고쳐라.
같은 핸들러에서 `PageUp`/`PageDown`(프레젠터 리모컨이 보내는 키)도 함께 지원하도록 추가하라.

---

## 3. [P1] 모바일에서 핵심 테이블이 잘려 안 보임

`index.html`에는 `<table class="spec-table">`가 2개 있다:
- `index.html:1487` — 7대 핵심 기능 OS별 제약·우회 매트릭스 (5열)
- `index.html:1615` — WBS / 32MM 공수 산정표

두 테이블 모두 가로 스크롤 컨테이너가 없고, `index.html:36`의 `body { overflow-x: hidden; }` 때문에 **모바일에서 4~5번째 열이 통째로 잘린다.** 하필 그 열에 가장 중요한 내용(우회 해결책, 공수 산정 근거)이 들어 있다.

**수정**:
1. `.table-scroll { overflow-x: auto; -webkit-overflow-scrolling: touch; }` 클래스를 CSS에 추가
2. 두 테이블을 각각 `<div class="table-scroll">`로 감싸라
3. 모바일에서 좌우로 스크롤 가능하다는 힌트(예: 오른쪽 페이드 그라데이션 또는 "← 좌우로 밀어보세요" 캡션)를 `@media (max-width: 960px)`에서만 표시하라

---

## 4. [P1] 시뮬레이터 광과민성·청각 안전 경고 추가

`index.html:523~535`의 `.screen-strobe`는 **0.3초 주기(약 3.3Hz)로 적색↔청색 전체화면 점멸**이다. 3Hz 이상 점멸은 WCAG 2.3.1 위반이자 실제 광과민성 발작 유발 대역이다. 또한 사이렌(`gain 0.4`, sawtooth)과 야생동물 기피 고주파(`19.5kHz, gain 0.2`)가 아무 예고 없이 재생된다.

**수정**:
1. 슬라이드 11(시뮬레이터) 상단에 **1회성 주의 배너**를 추가하라 — "본 시뮬레이터는 강한 화면 점멸과 고출력 경보음을 재생합니다. 광과민성 증상이 있거나 이어폰 착용 중이면 볼륨을 낮춰 주세요."
2. CSS에 `@media (prefers-reduced-motion: reduce) { .screen-strobe { animation: none; } }` 를 추가하라.
3. 사이렌·고주파 버튼 옆에 볼륨 주의 아이콘/문구를 배치하라.

---

## 5. [P1] 메타 태그 / 파비콘 / 딥링크 / 인쇄 스타일

`index.html`의 `<head>`(3~6줄)에는 `charset`, `viewport`, `title`밖에 없다.

**(a) 메타 태그 추가**
- `<meta name="description">` — 서비스 한 줄 소개
- Open Graph (`og:title`, `og:description`, `og:type`, `og:locale=ko_KR`)
- Twitter Card (`summary_large_image`)
- `<meta name="theme-color" content="#090d16">`
- 인라인 SVG data URI 파비콘 (🛡️ 방패 모티프, 외부 파일 금지)

**(b) 딥링크**
현재 18장 덱인데 URL 해시가 없어 특정 슬라이드를 링크로 공유하거나 새로고침 후 자리 복귀가 불가능하다.
- `render(idx)`에서 `history.replaceState`로 `#/{번호}` 를 갱신
- 페이지 로드 시 `location.hash`를 파싱해 해당 슬라이드로 시작
- `hashchange` 이벤트(브라우저 뒤/앞 버튼) 대응

**(c) 인쇄 스타일**
`@media print`가 전혀 없어 Ctrl+P 하면 현재 활성 슬라이드 1장만 출력된다. 제안서 특성상 PDF 제출 요구가 사실상 확실하다.
- 인쇄 시 모든 `.slide`를 `display: block`으로 펼치고 각 슬라이드마다 `page-break-after: always`
- 헤더의 진행바, 하단 플로팅 네비게이션 바, 시뮬레이터 버튼류는 `display: none`
- 배경을 흰색/텍스트를 진한 색으로 전환해 잉크 절약 및 가독성 확보

---

## 6. [P2] JavaScript 코드 위생

**(a) AudioContext 누수** — `index.html:1940` 부근 `toggleAnimalFrequency()`
토글할 때마다 `new AudioContext()`를 만들고 `close()`를 하지 않는다. 브라우저 AudioContext 상한(약 6개)에 도달하면 예외가 난다. 정지 시 반드시 `close()`하라. 사이렌/고주파가 **하나의 공유 AudioContext**를 재사용하도록 리팩터링하는 것이 더 낫다.

**(b) 중복 초기화** — `index.html:1821`(`DOMContentLoaded` 핸들러)과 `index.html:1850`(즉시 실행)에서 `initSlides()`가 두 번 호출된다. 하나만 남겨라.

**(c) `alert()` 12개 제거**
발표 중 네이티브 모달이 흐름을 끊고, iframe·키오스크 환경에서는 차단된다. 기존 다크 테마에 맞는 **인페이지 토스트 컴포넌트**를 만들어 전부 교체하라. (3~4초 후 자동 소멸, 우측 하단 스택, `role="status"`)

**(d) 접근성**
- `body`의 `user-select: none`(`index.html:37`)을 제거하거나 네비게이션 UI에만 한정하라. 지금은 덱 전체 텍스트를 복사할 수 없다.
- 비활성 슬라이드에 `aria-hidden="true"` / `inert` 적용
- 모달에 `Esc` 닫기, 포커스 트랩, `role="dialog"` `aria-modal="true"` 추가
- `onclick` 27개 중 `<div>`/`<span>`에 걸린 것이 있으면 `<button>`으로 바꾸거나 `role="button"` + `tabindex="0"` + 키보드 핸들러를 부여하라

---

## 7. [P2] worker.js 중복 구조 제거

`cloudflare/worker.js`는 `index.html` **전체 98KB를 자바스크립트 문자열로 복제**해 서빙한다. 현재는 두 파일 내용이 정확히 일치하지만, `index.html`을 한 번만 수정해도 배포본이 조용히 낡는다.

**다음 중 하나를 선택해 실행하고, 선택 이유를 설명하라**:
- **(권장)** `cloudflare/worker.js`를 삭제하고 **Cloudflare Workers Static Assets** 또는 **Cloudflare Pages** 방식으로 전환. `wrangler.toml`에 assets 디렉터리만 지정하면 되며 빌드가 불필요하다. `README.md`의 배포 안내(3번 항목)도 함께 갱신하라.
- **(차선)** 유지하되 `scripts/build-worker.js`를 추가해 `index.html`을 읽어 `worker.js`를 **자동 생성**하도록 하고, README에 "index.html 수정 후 반드시 이 스크립트를 실행" 문구를 넣어라.

**⚠️ 이번 작업에서 `index.html`을 수정했으므로, 어떤 방식을 택하든 배포본이 최신 `index.html`과 일치하는지 반드시 확인하라.**

---

## 8. [P2] 배포 설정 및 저장소 파일

**(a) `vercel.json`** — 현재 `cleanUrls`만 있다. 다음 헤더를 추가하라:
- `X-Content-Type-Options: nosniff`
- `Referrer-Policy: strict-origin-when-cross-origin`
- `Permissions-Policy` — 이 정적 덱이 실제로 쓰지 않는 기능(camera, geolocation 등)은 차단
- `Content-Security-Policy` — 단, 인라인 `<style>`/`<script>`를 쓰므로 `'unsafe-inline'` 허용이 필요하다. 페이지가 깨지지 않게 신중히 작성하고, 적용 후 실제로 렌더링·시뮬레이터가 정상 동작하는지 확인하라.
- `index.html`에 적절한 `Cache-Control`

**(b) 누락 파일 생성**: `.gitignore`(macOS `.DS_Store`, 에디터 파일, `node_modules`), `LICENSE`(라이선스 종류는 나에게 물어보고 결정하라)

---

## 9. [콘텐츠 보강] TECHNICAL_SPEC.md에 누락된 심사 필수 항목 추가

명세서가 기술·공수는 촘촘한데, **사업화 심사에서 반드시 나오는 질문**에 대한 답이 비어 있다. 아래 항목을 조사해 추가하라.

### (a) 위치정보법 대응 — 가장 큰 누락 ⚠️
`TECHNICAL_SPEC.md` §3.3(앱스토어 심사·법률 준수)은 아청법과 Apple Critical Alerts만 다룬다. 그러나 이 서비스는 「위치정보의 보호 및 이용 등에 관한 법률」상 **위치정보사업 허가(방송통신위원회) + 위치기반서비스사업 신고**가 사업 개시 전 필수다. 미신고 운영은 형사처벌 대상이며 심사에서 거의 확실히 질문받는다.

§3.3에 새 항목으로 추가하라: 허가/신고 구분, 소관 기관, 예상 소요 기간, 개인위치정보 동의 획득 절차(8세 이하 아동·피성년후견인 특례 포함), 위치정보 파기 정책, 개인위치정보 열람·고지 의무.

### (b) 통신비밀보호법 — 스텔스 30초 녹음
본인이 대화 당사자면 일방 당사자 녹음으로 합법이지만, **주변 제3자 대화가 함께 녹음되는 시나리오**의 법적 근거가 명세에 전혀 없다. S3 암호화 보관 기간, 자동 파기 정책, 수사기관 제출 시 영장주의 준수 절차를 §3.3에 명시하라.

### (c) 오경보(False Positive) KPI
`README.md`에 "**오경보 90% 차단**"이라고 적혀 있으나 산출 근거도 측정 방법도 없다. 119/112 연계 서비스는 오출동 비용이 곧 사업 리스크다. §4에 기능별 목표 FP율, 측정 방법, 필드테스트 검증 시나리오를 표로 추가하라.

### (d) 운영 비용(TCO) 추정 — 지자체 심사의 핵심
32MM 개발 공수는 있으나 **운영비 항목이 통째로 없다.** 지자체 사업은 개발비보다 3년 운영비를 먼저 본다.
§4에 새 절을 만들어 MAU 1만 / 5만 / 10만 시나리오별 월간 운영비를 산정하라. 최소 포함 항목: Kafka + Redis Cluster + PostGIS 인스턴스, WebSocket 상시 연결 서버, S3 오디오 스토리지, Mapbox GL 지도 로드 과금, Kakao 로컬/내비/알림톡 API 쿼터 과금, 공공데이터포털 트래픽 한도, 관제 인력 인건비.

### (e) 경쟁·유사 서비스 비교
안전드림(경찰청), 112 긴급신고 앱, Apple 위성 SOS, Life360, 제주도 기존 관광안전 서비스 대비 차별점 표를 추가하라. "왜 기존 것으로는 안 되는가"에 대한 답이 현재 덱 전체에 없다. **`index.html`에도 슬라이드로 추가할지 나에게 먼저 물어보라** (슬라이드 개수가 바뀌므로 승인 필요).

### (f) P2P 메시 실측 근거
"반경 45m BLE 릴레이"가 시뮬레이터 문구로만 등장하고 근거가 없다. §3.2의 5번 항목(오프라인 P2P 메시)에 곶자왈 수목 밀도 환경에서의 실제 BLE 도달거리 추정, 멀티홉 전파 지연, iOS 백그라운드 광고 15분 지연 시 실효 커버리지를 수치로 보강하라. **현재 덱에서 가장 공격받기 쉬운 지점이다.**

### (g) 개인정보 영향평가(PIA) 및 데이터 생명주기
수집 항목 / 보유 기간 / 파기 시점 / 제3자 제공(119·112·보호자) 근거를 표로 정리한 절을 추가하라.

---

## 10. 작업 방식 및 산출물

**진행 순서**: 1 → 2 → 3 → 4 → 5 → 6 → 7 → 8 → 9

**규칙**:
- **§1(성범죄자 알림e 정합화)을 반드시 가장 먼저 완료하고, 그 결과를 나에게 보고한 뒤 다음으로 진행하라.** 법적 리스크가 걸린 항목이라 내 확인이 필요하다.
- §9는 조사가 필요한 내용이다. **확실하지 않은 법령 조항·기관명·요금 수치는 지어내지 말고**, 근거가 불확실한 부분은 `[검증 필요]` 태그를 달아 표시하라. 특히 법령 조항 번호와 클라우드 요금은 추정치임을 명시하라.
- 파일을 수정할 때마다 **무엇을 왜 바꿨는지 한 줄로** 알려라.
- 큰 구조 변경(슬라이드 추가/삭제, worker.js 삭제, 라이선스 선택)은 **실행 전에 나에게 물어보라.**

**최종 산출물**:
1. 수정된 파일들
2. **변경 요약표** — 파일 / 변경 내용 / 이유
3. **검증 체크리스트** 실행 결과:
   - [ ] 브라우저 콘솔 에러 0건
   - [ ] 18장 슬라이드 전부 정상 표시
   - [ ] 키보드 ← → Space Home PageUp PageDown 전부 동작
   - [ ] URL 해시 딥링크 + 새로고침 시 자리 복귀 동작
   - [ ] 모바일 폭(375px)에서 두 테이블 가로 스크롤 동작
   - [ ] 인쇄 미리보기에서 18장 전부 출력
   - [ ] 시뮬레이터 사이렌/고주파 반복 토글 10회 후에도 정상 동작 (AudioContext 누수 없음)
   - [ ] 덱 전체에서 "성범죄자 알림e"를 기능으로 홍보하는 문구가 0건 (슬라이드 14 법적 검토 매트릭스의 서술은 예외로 유지)
   - [ ] `cloudflare/worker.js` 배포본이 최신 `index.html`과 일치 (유지하기로 한 경우)
4. **미해결/보류 항목**과 그 이유
